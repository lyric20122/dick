module.exports = slugifyFiles;

function slugifyFiles() {
	// each(function(file, filename) {
//    var extension = filename.indexOf('.html');
//    if (extension !== -1) {
//      return slugifyPath(filename.substr(0, extension)) + '.html';
//    } else {
//      return slugifyPath(filename);
//    }
//  })
	return function() {

	}
}
